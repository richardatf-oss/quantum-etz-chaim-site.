<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Quantum Etz Chaim</title>
  <meta name="description" content="Quantum Etz Chaim — a living map of sefirot, fields, and resonance." />

  <style>
    :root{
      --bg: #05060a;
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.72);
      --glass: rgba(8,10,18,.45);
      --stroke: rgba(255,255,255,.14);
      --shadow: 0 20px 60px rgba(0,0,0,.55);
    }
    *{ box-sizing:border-box; }
    html,body{ height:100%; margin:0; background:var(--bg); color:var(--text); font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans", "Helvetica Neue", sans-serif; }
    a{ color:inherit; text-decoration:none; }

    .hero{
      min-height:100svh;
      display:grid;
      place-items:center;
      position:relative;
      overflow:hidden;
      padding: clamp(18px, 4vw, 44px);
    }

    /* Background image */
    .hero::before{
      content:"";
      position:absolute; inset:-2%;
      background:
        radial-gradient(1200px 700px at 50% 25%, rgba(90,170,255,.18), transparent 60%),
        radial-gradient(900px 600px at 30% 70%, rgba(255,170,90,.14), transparent 60%),
        url("assets/glowing-etz-chaim.webp");
      background-size: cover;
      background-position: center;
      filter: saturate(1.05) contrast(1.05);
      transform: scale(1.02);
      z-index:0;
    }

    /* Readability overlay */
    .hero::after{
      content:"";
      position:absolute; inset:0;
      background:
        radial-gradient(900px 500px at 50% 35%, rgba(0,0,0,.25), rgba(0,0,0,.72)),
        linear-gradient(180deg, rgba(0,0,0,.25), rgba(0,0,0,.82));
      z-index:1;
    }

    .card{
      position:relative;
      z-index:2;
      width:min(980px, 100%);
      border: 1px solid var(--stroke);
      background: var(--glass);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border-radius: 22px;
      box-shadow: var(--shadow);
      padding: clamp(18px, 4vw, 34px);
    }

    .topline{
      display:flex;
      gap:12px;
      align-items:center;
      justify-content:space-between;
      flex-wrap:wrap;
      margin-bottom: 16px;
    }
    .mark{
      display:flex; gap:10px; align-items:baseline; flex-wrap:wrap;
      letter-spacing:.2px;
    }
    .mark strong{ font-size: 14px; text-transform:uppercase; opacity:.9; }
    .mark span{ font-size: 14px; color: var(--muted); }

    h1{
      margin: 8px 0 10px;
      font-size: clamp(34px, 5vw, 62px);
      line-height: 1.02;
      letter-spacing: -0.02em;
    }
    .subtitle{
      margin: 0 0 18px;
      font-size: clamp(15px, 1.7vw, 18px);
      color: var(--muted);
      line-height: 1.6;
      max-width: 70ch;
    }

    .cta{
      display:flex;
      gap: 12px;
      flex-wrap:wrap;
      margin-top: 16px;
    }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      padding: 12px 16px;
      border-radius: 14px;
      border: 1px solid var(--stroke);
      background: rgba(255,255,255,.06);
      transition: transform .15s ease, background .15s ease, border-color .15s ease;
      font-weight: 650;
      letter-spacing:.2px;
    }
    .btn:hover{ transform: translateY(-1px); background: rgba(255,255,255,.10); border-color: rgba(255,255,255,.22); }
    .btn.primary{
      background: rgba(255,255,255,.14);
      border-color: rgba(255,255,255,.26);
    }

    .micro{
      margin-top: 14px;
      color: rgba(255,255,255,.58);
      font-size: 13px;
      display:flex;
      gap:14px;
      flex-wrap:wrap;
      align-items:center;
    }
    .pill{
      border: 1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.18);
      padding: 6px 10px;
      border-radius: 999px;
    }

    @media (prefers-reduced-motion: reduce){
      .btn{ transition:none; }
      .btn:hover{ transform:none; }
    }
  </style>
</head>

<body>
  <main class="hero">
    <section class="card" aria-label="Quantum Etz Chaim landing">
      <div class="topline">
        <div class="mark">
          <strong>Quantum Etz Chaim</strong>
          <span>עץ חיים קוונטי</span>
        </div>
        <div class="pill">A living map of resonance</div>
      </div>

      <h1>The Tree, as Field.</h1>
      <p class="subtitle">
        Where Kabbalah meets quantum imagination: sefirot as harmonics, pathways as phase,
        and consciousness as the quiet measurement that turns light into story.
      </p>

      <div class="cta">
        <a class="btn primary" href="/explore">Enter the Tree</a>
        <a class="btn" href="/manifesto">Read the Manifesto</a>
        <a class="btn" href="/build">Build With Us</a>
      </div>

      <div class="micro">
        <span>Minimal. Fast. Sacred-tech.</span>
        <span>•</span>
        <span>Designed for Netlify + GitHub.</span>
      </div>
    </section>
  </main>
</body>
</html>
